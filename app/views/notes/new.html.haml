.m-auto{ class: "lg:w-1/2 md:w-3/4" }
  = render Common::Layout::TitleComponent.new(title: "New Note", priority: 1)
  .card= render @note.notable
  %turbo-frame{:id => "new_note"}
    = render Note::FormComponent.new(note: @note)