= form_with(model: opportunity_metric, class: "contents") do |form|
  - if opportunity_metric.errors.any?
    #error_explanation.bg-red-50.text-red-500.px-3.py-2.font-medium.rounded-lg.mt-3
      %h2
        = pluralize(opportunity_metric.errors.count, "error")
        prohibited this opportunity_metric from being saved:
      %ul
        - opportunity_metric.errors.each do |error|
          %li= error.full_message
  .inline
    = form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer"
